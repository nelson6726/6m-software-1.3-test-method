//Control flow or control structures determine the flow of the program based on the condition

// Comparison operators - > , <, ==, ===, >=, <= ...
// Logical Operators - && (AND), || (OR), !(NOT)

//1. 
let num2=10;
do {
    console.log("HappyBirthday");
    num2--;
} while (num2>=5);

let isDark = false;
let isTall = true;
let isHandsome = true;

console.log(isDark && isTall);
console.log